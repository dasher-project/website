name: Link Check

on:
  schedule:
    # Weekly, Monday 04:23 UTC (off-peak to avoid GitHub Actions congestion)
    - cron: '23 4 * * 1'
  workflow_dispatch:

jobs:
  link-check:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build site
        run: npm run build

      - name: Check YouTube videos
        run: npm run check:videos

      - name: Run lychee
        uses: lycheeverse/lychee-action@v2
        with:
          args: >-
            --no-progress
            --max-retries 3
            --exclude 'localhost'
            --exclude '127\.0\.0\.1'
            dist/
          fail: true
